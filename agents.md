# Fifty-Four エージェント

このドキュメントでは、`fifty-four` プロジェクトに実装されているエージェントについて説明します。

## 1. LSPサーバーエージェント

`backend.rs` に実装されている Language Server Protocol (LSP) の基本的なサーバー機能を提供するエージェントです（`main.rs` はエントリポイントとしてサーバー起動処理のみを行い、`Backend` 構造体とハンドラ実装は `backend.rs` に切り出されています）。

### 主な機能

- **初期化 (`initialize`)**: LSPクライアントからの初期化要求に応答し、サーバーが提供する機能（capabilities）を通知します。現在はセマンティックトークンの提供を宣言しています。
- **初期化完了 (`initialized`)**: クライアントの初期化が完了した際に通知を受け、ログメッセージを送信します。
- **シャットダウン (`shutdown`)**: サーバーのシャットダウン要求を正常に処理します。
- **セマンティックトークン (`semantic_tokens_full`)**:
  ドキュメント全体のセマンティックトークン（構文ハイライト情報）を提供する機能です。現在は空のトークンを返す最小限の実装ですが、将来的に
  `会話ハイライトエージェント` と連携して動作します。

## 2. 会話ハイライトエージェント

`highlight.rs` に実装されている、会話テキストのハイライト機能を提供するエージェントです。

### 主な機能

- **トークン化 (`tokenize_conversation`)**: 与えられたテキストを形態素解析し、ハイライト用のトークン列を生成します。
    - `lindera` という形態素解析ライブラリを利用しています（`lindera` feature flag が有効な場合）。
    - 品詞（名詞、動詞、形容詞など）に基づいて、各トークンに `variable`, `function`, `keyword` などの種類を割り当てます。
    - `lindera` が無効な場合は、空のトークン列を返すスタブ実装として動作します。

### トークン情報

生成されるトークン (`HighlightToken`) は以下の情報を持ちます。

- `start`: トークンの開始位置（バイトオフセット）
- `length`: トークンの長さ（バイト単位）
- `kind`: トークンの種類（例: "keyword", "string", "function"）
